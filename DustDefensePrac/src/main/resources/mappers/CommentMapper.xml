<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.domain.comment.CommentMapper">

	<sql id="commentColumns">
		  idx
		, board_idx
		, content
		, writer
		, delete_yn
		, insert_time
		, update_time
		, delete_time
	</sql>

	<insert id="insertComment" parameterType="com.example.demo.common.dto.CommentDto">
		INSERT INTO tb_comment (
			board_idx
			, content
			, writer
			, delete_yn
			, insert_time
			, update_time
			, delete_time
			
		) VALUES (
			#{boardIdx}
			, #{content}
			, #{writer}
			, 0
			, SYSDATE
			, NULL
			, NULL
		)
	</insert>

	<select id="selectCommentDetail" parameterType="long" resultType="com.example.demo.common.dto.CommentDto">
		SELECT
			<include refid="commentColumns" />
		FROM
			tb_comment
		WHERE
			delete_yn = 0
		AND
			idx = #{idx}
	</select>

	<update id="updateComment" parameterType="com.example.demo.common.dto.CommentDto">
		UPDATE tb_comment
		SET
			  update_time = SYSDATE
			, content = #{content}
			, writer = #{writer}
		WHERE
			idx = #{idx}
	</update>

	<update id="deleteComment" parameterType="long">
		UPDATE tb_comment
		SET
			  delete_yn = 1
			, delete_time = SYSDATE
		WHERE
			idx = #{idx}
	</update>

	<select id="selectCommentList" parameterType="com.example.demo.common.dto.CommentDto" resultType="com.example.demo.common.dto.CommentDto">
		SELECT
			<include refid="commentColumns" />
		FROM
			tb_comment
		WHERE
			delete_yn = 0
		AND
			board_idx = #{boardIdx}
		ORDER BY
			idx DESC,
			insert_time DESC
	</select>

	<select id="selectCommentTotalCount" parameterType="com.example.demo.common.dto.CommentDto" resultType="int">
		SELECT
			COUNT(*)
		FROM
			tb_comment
		WHERE
			delete_yn = 0
		AND
			board_idx = #{boardIdx}
	</select>
	

</mapper>